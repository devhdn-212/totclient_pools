-- MASTER

CREATE TABLE tbl_admin (
	username varchar(30) NOT NULL,
	"password" varchar(250) NULL,
	idadmin varchar(30) NULL,
	"name" varchar(50) NULL,
	statuslogin varchar(1) NULL,
	lastlogin timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	joindate date DEFAULT CURRENT_TIMESTAMP NOT NULL,
	ipaddress varchar(20) DEFAULT ''::character varying NULL,
	timezone varchar(30) DEFAULT ''::character varying NULL,
	createadmin varchar(30) NULL,
	createdateadmin timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updateadmin varchar(30) DEFAULT ''::character varying NULL,
	updatedateadmin timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	CONSTRAINT tbl_admin_pk PRIMARY KEY (username)
);
CREATE TABLE tbl_adminrole (
	idadminrole varchar(30) NOT NULL,
	nmadminrole varchar(50) NULL,
	ruleadmin text NULL,
	createadminrole varchar(30) DEFAULT ''::character varying NULL,
	createdateadminrole timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updateadminrole varchar(30) DEFAULT ''::character varying NULL,
	updatedateadminrole timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	CONSTRAINT tbl_adminrole_unique UNIQUE (idadminrole)
);
CREATE TABLE tbl_clientrule (
	idclientrule varchar(30) NOT NULL,
	nmclientrule varchar(50) NULL,
	ruleclient text NULL,
	createclientrule varchar(30) DEFAULT ''::character varying NULL,
	createdateclientrule timestamp DEFAULT CURRENT_TIMESTAMP  NULL,
	updateclientrule varchar(30) DEFAULT ''::character varying NULL,
	updatedateclientrule timestamp DEFAULT CURRENT_TIMESTAMP  NULL,
	CONSTRAINT tbl_clientrule_unique UNIQUE (idclientrule)
);
CREATE TABLE tbl_currency (
	idcurr varchar(20) NOT NULL,
	typecurr varchar(10) DEFAULT ''::character varying NOT NULL,
	status char(1) NOT NULL CHECK (status IN ('Y', 'N')),
	createcurr varchar(30) DEFAULT ''::character varying NULL,
	createdatecurr timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatecurr varchar(30) DEFAULT ''::character varying NULL,
	updatedatecurr timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	CONSTRAINT tbl_currency_unique UNIQUE (idcurr)
);
CREATE TABLE tbl_bank (
    idbank varchar(10) NOT NULL,
	typebank varchar(20) NOT NULL,
	nmbank varchar(50) NULL,
	bankstatus varchar(1) DEFAULT 'Y'::character varying NULL,
	createbank varchar(30) DEFAULT ''::character varying NULL,
	createdatebank timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatebank varchar(30) DEFAULT ''::character varying NULL,
	updatedatebank timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_tbl_bank_unique UNIQUE (idbank)
);

CREATE TABLE tbl_mst_domain (
	iddomain varchar NOT NULL,
	nmdomain varchar(70) NULL,
	tipedomain varchar(50) NULL,
	statusdomain varchar(15) NULL,
	createdomain varchar(70) NULL,
	createdatedomain timestamp NULL,
	updatedomain varchar(70) DEFAULT 'root'::character varying NULL,
	updatedatedomain timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_mst_domain_pk PRIMARY KEY (iddomain)
);
CREATE TABLE tbl_company (
	idcompany varchar(10) NOT NULL,
	idcurrdef varchar(20) NOT NULL,
	compname varchar(50) NULL,
	endjoin timestamp NULL,
	amountcomp numeric(36, 18) NOT NULL DEFAULT 0,
	compstatus varchar(1) DEFAULT 'Y'::character varying NULL,
	createcomp varchar(30) DEFAULT ''::character varying NULL,
	createdatecomp timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatecomp varchar(30) DEFAULT ''::character varying NULL,
	updatedatecomp timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_company_unique UNIQUE (idcompany)
);
CREATE TABLE tbl_company_wallet (
    idcompwallet varchar(100) NOT NULL,
	idcompany varchar(10) NOT NULL,
	idcurr varchar(20) NOT NULL,
	amountcompwallet numeric(36, 18) NOT NULL DEFAULT 0,
	compwalletstatus varchar(1) DEFAULT 'Y'::character varying NULL,
	createcompwallet varchar(30) DEFAULT ''::character varying NULL,
	createdatecompwallet timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatecompwallet varchar(30) DEFAULT ''::character varying NULL,
	updatedatecompwallet timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_company_wallet_unique UNIQUE (idcompwallet)
);
CREATE TABLE tbl_company_admin (
    idcompadmin varchar(100) NOT NULL,
	idcompany varchar(10) NOT NULL,
	idclientrule varchar(30) NOT NULL,
	usernamecompadmin varchar(30) NOT NULL,
	namecompadmin varchar(50) NULL,
    passcompadmin varchar(250) NULL,
    lastlogincompadmin timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    ipaddresscompadmin varchar(20) DEFAULT ''::character varying NULL,
	compadminstatus varchar(1) DEFAULT 'Y'::character varying NULL,
	createcompadmin varchar(30) DEFAULT ''::character varying NULL,
	createdatecompadmin timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatecompadmin varchar(30) DEFAULT ''::character varying NULL,
	updatedatecompadmin timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_company_admin_unique UNIQUE (idcompadmin)
);

// AGEN schema sendiri berdasarkan idcompany
CREATE TABLE tbl_mst_member (
    idmember varchar(100) NOT NULL,
	usernamemember varchar(30) NOT NULL,
	passmember varchar(250) NULL,
	namemember varchar(50) NULL,
	hpmember varchar(30) DEFAULT ''::character varying NULL,
	emailmember varchar(100) DEFAULT ''::character varying NULL,
    lastloginmember timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    ipaddressmember varchar(20) DEFAULT ''::character varying NULL,
	statusmember varchar(1) DEFAULT 'Y'::character varying NULL,
	createmember varchar(30) DEFAULT ''::character varying NULL,
	createdatemember timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatemember varchar(30) DEFAULT ''::character varying NULL,
	updatedatemember timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_member_unique UNIQUE (idmember)
);
CREATE TABLE tbl_mst_member_bank (
    idmemberbank varchar(150) NOT NULL,
    idmember varchar(100) NOT NULL,
    idbank varchar(10) NOT NULL,
	norek varchar(50) DEFAULT ''::character varying NULL,
	nmrekening varchar(100) DEFAULT ''::character varying NULL,
	creatememberbank varchar(30) DEFAULT ''::character varying NULL,
	createdatememberbank timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatememberbank varchar(30) DEFAULT ''::character varying NULL,
	updatedatememberbank timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_member_bank_unique UNIQUE (idmemberbank)
);
CREATE TABLE tbl_mst_member_wallet (
    idmemberwallet varchar(150) NOT NULL,
    idmember varchar(100) NOT NULL,
    idcurr varchar(20) NOT NULL,
    amount_creditwallet numeric(36, 18) NOT NULL DEFAULT 0,
    amount_debitwallet numeric(36, 18) NOT NULL DEFAULT 0,
	creatememberwallet varchar(30) DEFAULT ''::character varying NULL,
	createdatememberwallet timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatememberwallet varchar(30) DEFAULT ''::character varying NULL,
	updatedatememberwallet timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_member_wallet_unique UNIQUE (idmemberwallet)
);


CREATE TABLE tbl_mst_company_walletbank (
	idcompwalletbank varchar(150) NOT NULL,
	idcurr varchar(20) NOT NULL,
	idbank varchar(10) DEFAULT ''::character varying NULL,
	categorycompwalletbank varchar(20) DEFAULT ''::character varying NULL,
	account_number varchar(150) DEFAULT ''::character varying NULL,
	account_name varchar(150) DEFAULT ''::character varying NULL,
	networkcrypto varchar(20) DEFAULT ''::character varying NULL,
	addressqris varchar(150) DEFAULT ''::character varying NULL,
	amountcompwalletbank_credit numeric(36, 18) DEFAULT 0 NOT NULL,
	amountcompwalletbank_debit numeric(36, 18) DEFAULT 0 NOT NULL,
	statuscompwalletbank varchar(1) DEFAULT 'Y'::character varying NULL,
	notecompwalletbank varchar(150) DEFAULT ''::character varying NULL,
	effective_from timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	effective_to timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	createcompwalletbank varchar(30) DEFAULT ''::character varying NULL,
	createdatecompwalletbank timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatecompwalletbank varchar(30) DEFAULT ''::character varying NULL,
	updatedatecompwalletbank timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_mst_company_walletbank_unique UNIQUE (idcompwalletbank)
);




CREATE TABLE tbl_trx_company_walletbank (
	idtrxcompwalletbank varchar(150) NOT NULL,
	typetrx varchar(10) NOT NULL,
	idcurr varchar(20) NOT NULL,
	datetrx date NOT NULL,
	wallet_idcompwalletbank varchar(150) NULL,
	wallet_snapshot varchar(200) NULL,
	source_wallet_idcompwalletbank varchar(150) NULL,
	source_wallet_snapshot varchar(200) NULL,
	destination_wallet_idcompwalletbank varchar(150) NULL,
	destination_wallet_snapshot varchar(200) NULL,
	amount_trx numeric(36, 18) DEFAULT 0 NOT NULL,
	admin_fee numeric(36, 18) DEFAULT 0 NOT NULL,
	rate_trx numeric(36, 18) DEFAULT 0 NOT NULL,
	amountfinal_trx numeric(36, 18) DEFAULT 0 NOT NULL,
	amountratefinal_trx numeric(36, 18) DEFAULT 0 NOT NULL,
	status varchar(10) DEFAULT 'DRAFT'::character varying NOT NULL,
	title varchar(150) DEFAULT ''::character varying NULL,
	note varchar(150) DEFAULT ''::character varying NULL,
	approved_by varchar(30) NULL,
	approved_at timestamp NULL,
	createtrx varchar(30) DEFAULT ''::character varying NULL,
	createdatetrx timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updatetrx varchar(30) DEFAULT ''::character varying NULL,
	updatedatetrx timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_trx_company_walletbank_unique UNIQUE (idtrxcompwalletbank)
);



CREATE TABLE tbl_ledger_company (
	idledger varchar(150) NOT NULL,
	ref_idtrx varchar(150) NOT NULL,
	ref_table varchar(150) NOT NULL,
	idcompwalletbank varchar(150) DEFAULT ''::character varying NULL,
	typeledger varchar(10) NOT NULL,
	dateledger date NOT NULL,
	idcurr varchar(20) NOT NULL,
	amount_credit numeric(36, 18) DEFAULT 0 NOT NULL,
	amount_debit numeric(36, 18) DEFAULT 0 NOT NULL,
	createledger varchar(30) DEFAULT ''::character varying NULL,
	createdateledger timestamp DEFAULT CURRENT_TIMESTAMP NOT NULL,
	updateledger varchar(30) DEFAULT ''::character varying NULL,
	updatedateledger timestamp DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT tbl_ledger_company_unique UNIQUE (idledger)
);